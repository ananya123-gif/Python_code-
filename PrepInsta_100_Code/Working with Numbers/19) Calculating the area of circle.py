# Calculating the area of circle

# The formula for evaluating the area of circle is
# Area of circle using radius=   Pi*r*r    ( r is the radius of circle)

import math
def area_of_circle(r):
    return math.pi*r*r

r=int(input())
print(area_of_circle(r))
